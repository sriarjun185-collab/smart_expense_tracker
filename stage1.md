Stage 1 – Create the Project

Use this prompt first:

Create a complete MERN Stack project named "Smart Expense Tracker".

Use:
- React + Vite
- Tailwind CSS
- Node.js
- Express.js
- MongoDB
- JWT Authentication

Create the project structure only.

Frontend:
client/

Backend:
server/

Configure all folders.

Configure package.json files.

Install all required dependencies.

Create environment files.

Configure React Router.

Configure Express server.

Configure MongoDB connection.

Do not implement features yet.

The project should run successfully using:

npm install

npm run dev

Ensure there are no build errors.

If any dependency is missing, install it automatically.

If any error occurs, fix it automatically before continuing.